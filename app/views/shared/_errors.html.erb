<% if obj && obj.errors.any? %> 
  <div class="alert alert-danger">
    <p>You have: <%= pluralize(obj.errors.count, "error") %></p>
    <ul>
      <% obj.errors.full_messages.each do |msg|%>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>